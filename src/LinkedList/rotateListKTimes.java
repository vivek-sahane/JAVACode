package LinkedList;

public class rotateListKTimes {
//        public ListNode rotateRight(ListNode head, int k) {
//            if (head == null || head.next == null || k == 0)
//                return head;
//
//            ListNode temp = head;
//            int length = 1;
//            while (temp.next != null) {
//                temp = temp.next;
//                length++;
//            }
//            temp.next = head; // Make it a circular linked list
//
//            int stepsToMove = length - (k % length);
//            for (int i = 0; i < stepsToMove; i++) {
//                temp = temp.next;
//            }
//
//            head = temp.next;
//            temp.next = null; // Break the connection to form a proper linked list
//
//            return head;
//        }
//

}
